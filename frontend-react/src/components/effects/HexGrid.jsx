/**
 * HexGrid - radial gradient background overlay
 *
 * 4-layer radial gradient giving the JARVIS ambient glow.
 * Pure CSS, no JS animation. Ported from index.html lines 78-89.
 */
export default function HexGrid() {
  return <div className="hex-grid-bg" />;
}
